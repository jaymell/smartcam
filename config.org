[video]

# source options:
# device number -- eg 0
# 'device,non-default' to help avoid built-in 
#   webcams from being read; 
# just 'device' otherwise to use first device found
source=0

#video_format=mp42
video_format=mjpg

# time (in seconds) between background shots:
motion_timeout=30

# sets interval at which main loop runs -- 14 appears to be about the max for current cam:
fps=14
